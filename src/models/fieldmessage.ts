export interface FieldMessage {
    fieldName : string;
    message : string;
}
/*Este fieldMessage corresponde ao fildmessage no backend. Ele vai guardar as mensagens de erro de validação
geradas la no backend */